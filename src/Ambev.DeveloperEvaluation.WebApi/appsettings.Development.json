{
  "_comment": "Dev-only overrides — secrets live in dotnet user-secrets / env vars (ConnectionStrings__DefaultConnection, Jwt__SecretKey). The placeholders below let the app start when a developer forgets to set them, but tools like 'dotnet user-secrets set' or .env take precedence.",
  "ConnectionStrings": {
    "DefaultConnection": "",
    "Redis": ""
  },
  "Jwt": {
    "SecretKey": ""
  },
  "Cors": {
    "AllowedOrigins": [ "http://localhost:5173", "http://localhost:4200" ]
  },
  "OpenTelemetry": {
    "OtlpEndpoint": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
